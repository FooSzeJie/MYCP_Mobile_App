class Car {
  String id;
  String licensePlate;
  String color;
  String brand;
  // Car({ required this.licensePlate, required this.color, required this.brand});
  Car({required this.id, required this.licensePlate, required this.color, required this.brand});
}